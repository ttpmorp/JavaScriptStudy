const nome = 'Julia C Silva';

// Loop que percorre cada caractere da string 'nome'
for (let i = 0; i < nome.length; i++) {
    // Obtém o caractere na posição 'i' da string 'nome'
    const letra = nome[i];
    
    // Imprime a letra atual no console
    console.log(letra);
}

